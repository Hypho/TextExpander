# Agent Instructions

Read `.ledger/core/workflow.md`.

Also read `a.md`, `b.md`, `c.md`, `d.md`, `e.md`, `f.md`, `g.md`, `h.md`, `i.md`, `j.md`, and `k.md`.

```bash
bash .ledger/bin/ledger.sh guard pid
bash .ledger/bin/ledger.sh check --project
```

